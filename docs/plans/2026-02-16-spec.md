# 🏀 Ballin — 기능 명세서 (spec.md)

> **작성일:** 2026-02-16
> **프로젝트명:** Ballin (농구 룰 & 연습법 가이드)
> **상태:** 작성 중
> **참고:** 설계 방향과 "왜"에 대한 답은 [design.md](./2026-02-07-basketball-guide-design.md)를 참조

---

## 1. 목표

### 1.1 비전

> **코트 위의 백과사전** — 농구 룰과 연습법을 체계적으로 정리하여, 입문자부터 중급자까지 현장에서 바로 찾아보는 웹앱

### 1.2 핵심 문제 해결

| 현재 문제 | 목표 | 해결책 |
|-----------|------|--------|
| 농구 룰이 여러 소스에 흩어져 있음 | 원스톱 레퍼런스 | FIBA/NBA 통합 룰북 |
| FIBA/NBA 차이를 한 눈에 비교하기 어려움 | 즉시 비교 가능 | 탭 전환 + 비교 모드 |
| 연습법이 단편적, 루틴 조합이 없음 | 목적별 루틴 제공 | 드릴 → 루틴 조합 시스템 |
| 코트에서 스마트폰으로 찾기 불편 | 모바일 최적 경험 | 모바일 퍼스트 설계 |

### 1.3 Phase 범위

| Phase | 기간 | 범위 |
|-------|------|------|
| **P0 (MVP)** | 1단계 | 홈, 룰북 9개 카테고리, FIBA/NBA 탭 전환, YouTube 임베드, 모바일 반응형, 기본 레이아웃 |
| **P1** | 2단계 | 연습법 3개 카테고리, 용어사전, 통합 검색, 난이도 필터 |
| **P2** | 3단계 | FIBA/NBA 비교 모드, 추천 루틴, 북마크, 다크모드 |
| **P3** | 4단계 | 자체 영상 제작, 퀴즈/테스트, 커뮤니티 |

---

## 2. 핵심 기능

### P0 (MVP 필수)

- [x] F001: 공통 레이아웃 (Header + Footer + Breadcrumb)
- [x] F002: 홈 페이지
- [x] F003: 룰북 카테고리 목록 페이지
- [x] F004: 카테고리별 룰 목록 페이지
- [x] F005: 개별 룰 페이지 (MDX 렌더링)
- [x] F006: FIBA/NBA 탭 전환
- [x] F007: YouTube 영상 임베드
- [x] F008: 난이도 뱃지 (표시만, 필터 없음)
- [x] F009: 관련 룰 링크
- [x] F010: 모바일 반응형 레이아웃
- [x] F011: MDX 콘텐츠 파싱 시스템
- [x] F012: SEO (메타태그, OG 이미지, sitemap)

### P1 (콘텐츠 확장)

- [x] F020: 연습법 카테고리 목록 페이지
- [x] F021: 카테고리별 연습법 목록 페이지
- [x] F022: 개별 연습법 페이지 (MDX 렌더링)
- [x] F023: 용어사전 페이지
- [x] F024: 용어사전 검색 (클라이언트)
- [x] F025: 통합 검색 (Fuse.js)
- [x] F026: 난이도 필터
- [x] F027: 강조 박스 컴포넌트 (InfoBox)

### P2 (사용성 강화)

- [x] F030: FIBA/NBA 비교 모드 (나란히 보기)
- [x] F031: 추천 루틴 목록 페이지
- [x] F032: 개별 루틴 페이지
- [x] F033: 북마크 (로컬스토리지)
- [x] F034: 다크모드
- [x] F035: 목차 (Table of Contents) 자동 생성
- [x] F036: 스크롤 진행 표시기

### P3 (고도화) — 미구현

- [ ] F040: 퀴즈 / 셀프 테스트
- [ ] F041: 커뮤니티 (댓글/피드백)
- [ ] F042: PWA 지원 (오프라인 열람)
- [ ] F043: 자체 영상 연동

---

## 3. 데이터 구조

### 3.1 MDX 프론트매터 인터페이스

#### 룰 페이지

```typescript
interface RuleFrontmatter {
  title: string               // "트래블링 (Traveling)"
  category: RuleCategory      // "violations"
  difficulty: Difficulty       // "beginner" | "intermediate" | "advanced"
  summary: string             // 한 줄 요약 (120자 이내)
  tags: string[]              // ["바이올레이션", "드리블", "피벗풋"]
  relatedRules: string[]      // ["double-dribble", "pivot-foot"] — slug 기반
  videos: Video[]
  fiba: LeagueRuleDetail
  nba: LeagueRuleDetail
  confusingPoints?: string[]  // 자주 헷갈리는 포인트 (선택)
}

interface LeagueRuleDetail {
  description: string         // 해당 리그 기준 설명 (마크다운 허용)
  keyPoints: string[]         // 핵심 포인트 리스트
}

interface Video {
  url: string                 // YouTube URL
  title: string
  source?: string             // "NBA Official" | "FIBA" | 기타
}

type Difficulty = "beginner" | "intermediate" | "advanced"

type RuleCategory =
  | "game-basics"
  | "five-on-five"
  | "three-on-three"
  | "violations"
  | "fouls"
  | "positions"
  | "tactics-rules"
  | "sportsmanship"
  | "special-situations"
```

#### 연습법 페이지

```typescript
interface DrillFrontmatter {
  title: string               // "크로스오버 드리블 기초"
  category: DrillCategory     // "individual-skills"
  subcategory: string         // "ball-handling"
  difficulty: Difficulty
  duration: string            // "15분" — 사람이 읽는 형식
  durationMinutes: number     // 15 — 필터/정렬용 숫자
  equipment: string[]         // ["농구공 1개"]
  summary: string             // 한 줄 요약
  tags: string[]
  videos: Video[]
  commonMistakes: string[]    // 흔한 실수 리스트
  relatedDrills: string[]     // slug 기반
}

type DrillCategory =
  | "individual-skills"
  | "team-tactics"
  | "fitness"
```

#### 루틴 페이지

```typescript
interface RoutineFrontmatter {
  title: string               // "슈팅 30분 루틴"
  purpose: string             // "슈팅 감각 유지 및 향상"
  difficulty: Difficulty
  totalDuration: string       // "30분"
  totalDurationMinutes: number // 30
  targetAudience: string      // "초급~중급 슈터"
  tags: string[]
  drills: RoutineDrill[]
}

interface RoutineDrill {
  slug: string                // 연습법 MDX slug
  title: string               // 표시명 (MDX에서 자동 가져오기 실패 시 폴백)
  sets: number                // 세트 수
  reps?: string               // "10회" 또는 "30초"
  restSeconds: number         // 세트 간 쉬는 시간 (초)
  notes?: string              // "왼손으로만 실시"
}
```

#### 용어사전

```typescript
interface GlossaryFrontmatter {
  term: string                // "트래블링"
  termEn: string              // "Traveling"
  definition: string          // 간단한 정의
  category?: string           // "violation" | "offense" | "defense" | "general"
  relatedTerms?: string[]     // slug 기반
  relatedRules?: string[]     // 룰 slug 기반
  relatedDrills?: string[]    // 드릴 slug 기반
}
```

### 3.2 카테고리 메타데이터

```typescript
// lib/categories.ts

interface CategoryMeta {
  slug: string
  title: string
  titleEn: string
  description: string
  icon: string                // 이모지 또는 아이콘 이름
  order: number               // 목록 정렬 순서
}

const RULE_CATEGORIES: CategoryMeta[] = [
  { slug: "game-basics",        title: "기본 규칙",                titleEn: "Game Basics",         description: "코트, 점수, 선수 구성 등 농구의 기초", icon: "🏟️", order: 1 },
  { slug: "five-on-five",       title: "5대5 룰",                 titleEn: "5-on-5 Rules",        description: "정식 5대5 경기 운영 규칙",              icon: "🏀", order: 2 },
  { slug: "three-on-three",     title: "3대3 룰",                 titleEn: "3x3 Rules",           description: "3x3 전용 규칙과 운영 방식",             icon: "3️⃣", order: 3 },
  { slug: "violations",         title: "바이올레이션",             titleEn: "Violations",          description: "트래블링, 더블드리블 등 반칙",           icon: "🚫", order: 4 },
  { slug: "fouls",              title: "파울",                    titleEn: "Fouls",               description: "퍼스널, 테크니컬, 플래그런트 파울",     icon: "🤚", order: 5 },
  { slug: "positions",          title: "포지션별 역할 & 규칙",     titleEn: "Positions",           description: "가드, 포워드, 센터 역할과 관련 규칙",   icon: "👥", order: 6 },
  { slug: "tactics-rules",      title: "공격/수비 전술 관련 룰",   titleEn: "Tactics Rules",       description: "스크린, 존 디펜스, 차징 판정 등",       icon: "📋", order: 7 },
  { slug: "sportsmanship",      title: "경기 매너 & 스포츠맨십",   titleEn: "Sportsmanship",       description: "언스포츠맨라이크 파울, 페어플레이",     icon: "🤝", order: 8 },
  { slug: "special-situations", title: "특수 상황",               titleEn: "Special Situations",  description: "리플레이, 챌린지, 클락 오작동",         icon: "⚡", order: 9 },
]

const DRILL_CATEGORIES: CategoryMeta[] = [
  { slug: "individual-skills",  title: "개인 스킬",       titleEn: "Individual Skills",      description: "볼 핸들링, 슈팅, 패싱, 디펜스",   icon: "🎯", order: 1 },
  { slug: "team-tactics",       title: "팀 전술",         titleEn: "Team Tactics",            description: "픽앤롤, 존 디펜스, 세트 플레이",  icon: "🧩", order: 2 },
  { slug: "fitness",            title: "체력 & 컨디셔닝", titleEn: "Fitness & Conditioning",  description: "러닝, 근력, 민첩성, 유연성",      icon: "💪", order: 3 },
]
```

---

## 4. 기능 상세 명세

### 4.1 MDX 콘텐츠 시스템 (F011)

**콘텐츠 디렉토리 구조**

```
content/
├── rules/
│   ├── game-basics/
│   │   ├── court-dimensions.mdx
│   │   ├── scoring-system.mdx
│   │   └── player-composition.mdx
│   ├── violations/
│   │   ├── traveling.mdx
│   │   └── double-dribble.mdx
│   └── ...
├── training/
│   ├── individual-skills/
│   │   ├── ball-handling/
│   │   │   ├── crossover-basics.mdx
│   │   │   └── behind-the-back.mdx
│   │   └── shooting/
│   │       └── free-throw-routine.mdx
│   └── ...
├── routines/
│   ├── shooting-30min.mdx
│   └── ball-handling-20min.mdx
└── glossary/
    ├── traveling.mdx
    └── double-dribble.mdx
```

**MDX 파싱 유틸리티 (`lib/content.ts`)**

```typescript
// 모든 룰 가져오기 (목록 페이지용)
function getAllRules(): RuleMeta[]

// 카테고리별 룰 가져오기
function getRulesByCategory(category: RuleCategory): RuleMeta[]

// 개별 룰 가져오기 (MDX 본문 포함)
function getRuleBySlug(category: string, slug: string): {
  frontmatter: RuleFrontmatter
  content: MDXRemoteSerializeResult
} | null

// 연습법 동일 패턴
function getAllDrills(): DrillMeta[]
function getDrillsByCategory(category: DrillCategory): DrillMeta[]
function getDrillBySlug(category: string, slug: string): { ... } | null

// 루틴
function getAllRoutines(): RoutineMeta[]
function getRoutineBySlug(slug: string): { ... } | null

// 용어사전
function getAllGlossaryTerms(): GlossaryMeta[]

// 검색 인덱스 빌드 (빌드 타임)
function buildSearchIndex(): SearchDocument[]
```

**`RuleMeta` (목록 렌더링용 경량 타입)**

```typescript
interface RuleMeta {
  slug: string           // 파일명에서 추출 (e.g., "traveling")
  category: RuleCategory
  title: string
  summary: string
  difficulty: Difficulty
  tags: string[]
}
```

**파일명 → slug 규칙**

- 파일명: `kebab-case.mdx` → slug: `kebab-case`
- 디렉토리 구조가 URL을 결정:
  - `content/rules/violations/traveling.mdx` → `/rules/violations/traveling`
  - `content/training/individual-skills/ball-handling/crossover-basics.mdx` → `/training/individual-skills/crossover-basics`
  - 연습법의 subcategory(`ball-handling`)는 프론트매터에만 존재하고, URL에는 category까지만 반영

**MDX 렌더링**

- `next-mdx-remote` 사용 (빌드 타임 SSG)
- 커스텀 MDX 컴포넌트 매핑:
  - `<InfoBox type="warning|tip|confusing">` → 강조 박스
  - `<VideoEmbed url="..." title="..." />` → YouTube 임베드
  - `<DifficultyBadge level="beginner" />` → 난이도 뱃지
  - `<RuleCompare />` → FIBA/NBA 탭 전환 (프론트매터의 fiba/nba 데이터 사용)

**에러 처리**

- MDX 파일이 존재하지 않는 경우 → Next.js `notFound()` 반환 (404)
- 프론트매터 필수 필드 누락 → 빌드 타임 에러 (Zod 스키마 검증)
- `relatedRules`의 slug가 실제 파일과 매칭되지 않는 경우 → 해당 링크 숨김 처리, 콘솔 경고

### 4.2 공통 레이아웃 (F001)

**Header**

```typescript
interface HeaderProps {
  // props 없음, 전역 레이아웃
}

// 구성 요소:
// 1. 로고 (좌측) — 클릭 시 홈(/)으로 이동
// 2. 네비게이션 (중앙) — 룰북, 연습법, 루틴, 용어사전
// 3. 검색 아이콘 (우측)
// 4. 다크모드 토글 (우측)

// 모바일:
// - 햄버거 메뉴로 전환 (768px 이하)
// - 메뉴 오픈 시 오버레이 + 슬라이드 인
// - 메뉴 항목 클릭 시 자동 닫힘
```

**Breadcrumb**

```typescript
interface BreadcrumbProps {
  items: BreadcrumbItem[]
}

interface BreadcrumbItem {
  label: string
  href?: string    // 마지막 항목은 href 없음 (현재 위치)
}

// 예시: 홈 > 룰북 > 바이올레이션 > 트래블링
// items: [
//   { label: "홈", href: "/" },
//   { label: "룰북", href: "/rules" },
//   { label: "바이올레이션", href: "/rules/violations" },
//   { label: "트래블링" }  // 현재 페이지, 링크 없음
// ]

// 모바일에서: "< 바이올레이션" 형태로 한 단계 상위만 표시
```

**Footer**

- 프로젝트 소개 한 줄
- GitHub 링크
- 저작권 표시

### 4.3 홈 페이지 (F002)

**구성 요소**

1. **히어로 섹션** — 프로젝트 한 줄 소개 + "룰북 시작하기" CTA
2. **빠른 바로가기** — 4개 섹션 카드 (룰북, 연습법, 루틴, 용어사전)
3. **추천 콘텐츠** — 인기/추천 룰 카드 3~4개 (하드코딩)
4. **통합 검색바** — 히어로 하단에 배치

**추천 콘텐츠 선정 기준**

```typescript
// 하드코딩 추천 목록 (config/recommended.ts)
const RECOMMENDED_RULES: string[] = [
  "violations/traveling",
  "fouls/personal-fouls",
  "game-basics/scoring-system",
  "five-on-five/game-time",
]
```

### 4.4 룰북 목록 페이지 (F003, F004)

**카테고리 목록 (`/rules`)**

- 9개 카테고리를 카드 그리드로 표시
- 각 카드: 아이콘 + 제목 + 설명 + 해당 카테고리 룰 개수
- 정렬: `order` 필드 기준 고정 순서
- 클릭 → `/rules/[category]` 이동

**카테고리별 룰 목록 (`/rules/[category]`)**

- 해당 카테고리의 모든 룰을 카드 리스트로 표시
- 각 카드: 제목 + 한 줄 요약 + 난이도 뱃지
- 정렬: MDX 파일 내 `order` 프론트매터 (선택), 없으면 파일명 알파벳순

**RuleCard 컴포넌트**

```typescript
interface RuleCardProps {
  slug: string
  category: string
  title: string
  summary: string
  difficulty: Difficulty
}

// 동작:
// - 카드 전체가 클릭 가능 (Link 컴포넌트)
// - 호버: 카드 살짝 올라옴 (shadow 변화)
// - 난이도 뱃지: 좌측 상단
// - 제목: 메인 텍스트
// - 요약: 제목 아래 회색 텍스트 (2줄 초과 시 말줄임)
```

**에러 처리**

- 존재하지 않는 카테고리 → 404
- 카테고리에 룰이 0개 → "아직 콘텐츠가 없습니다. 곧 추가됩니다!" 메시지

### 4.5 개별 룰 페이지 (F005, F006, F007, F008, F009)

**URL 구조:** `/rules/[category]/[slug]`

**페이지 구성 (위에서 아래)**

1. Breadcrumb
2. 제목 + 난이도 뱃지
3. 한 줄 요약 (회색 텍스트)
4. **FIBA/NBA 탭 전환 영역** (F006)
5. MDX 본문 (해당 리그 기준 설명)
6. 자주 헷갈리는 포인트 (InfoBox, `confusingPoints` 있을 때만)
7. 관련 영상 (F007)
8. 관련 룰 링크 (F009)
9. 북마크 버튼

**FIBA/NBA 탭 전환 (F006)**

```typescript
interface RuleCompareProps {
  fiba: LeagueRuleDetail
  nba: LeagueRuleDetail
}

// 상태:
// - activeTab: "fiba" | "nba" (기본값: "fiba")
// - compareMode: boolean (기본값: false)

// 동작:
// 1. 탭 클릭 → activeTab 변경
// 2. URL 쿼리 파라미터로 상태 유지: /rules/violations/traveling?league=nba
//    - 직접 URL 입력 또는 공유 시 해당 탭으로 열림
//    - 쿼리 없으면 기본값 "fiba"
// 3. 탭 내용:
//    - description을 마크다운으로 렌더링
//    - keyPoints를 불릿 리스트로 표시
// 4. 탭 전환 시 스크롤 위치 유지 (탭 영역 상단으로 이동하지 않음)
// 5. 접근성: role="tablist", role="tab", aria-selected

// 비교 모드 (F030):
// - 토글 스위치 ON → FIBA와 NBA가 나란히 2컬럼으로 표시
// - 차이점 하이라이트: keyPoints에서 상대 리그에 없는 항목을 리그별 색상으로 강조
//   - 비교 로직: keyPoints 문자열 매칭 (완전 일치 기준)
//   - 양쪽 모두 있는 항목: 일반 표시
//   - FIBA 고유 항목: 파란색 배경(bg-blue-50/blue-950) + "FIBA만" 라벨
//   - NBA 고유 항목: 빨간색 배경(bg-red-50/red-950) + "NBA만" 라벨
// - 모바일 (768px 이하): 2컬럼 불가 → 위/아래 스택으로 전환 (FIBA 위, NBA 아래)
// - 비교 모드 상태도 URL 반영: ?league=compare
```

**YouTube 영상 임베드 (F007)**

```typescript
interface VideoEmbedProps {
  url: string       // YouTube URL (watch?v=xxx 또는 youtu.be/xxx)
  title: string
  source?: string
}

// 동작:
// 1. YouTube URL에서 video ID 추출
//    - https://youtube.com/watch?v=ID → ID
//    - https://youtu.be/ID → ID
//    - 그 외 형식 → 렌더링하지 않음 (콘솔 경고)
// 2. 성능 최적화: 썸네일 + 재생 버튼 표시 (lazy load)
//    - 클릭 시 실제 iframe 로드 (lite-youtube-embed 패턴)
//    - 썸네일: https://img.youtube.com/vi/{ID}/hqdefault.jpg
// 3. 반응형: 16:9 비율 유지 (aspect-ratio 또는 padding-top 트릭)
// 4. 여러 영상이 있는 경우: 세로로 나열 (카드 형태)
//    - 각 카드: 썸네일 + 제목 + source 뱃지

// 에러 처리:
// - 유효하지 않은 URL → 해당 영상 카드 숨김
// - 썸네일 로드 실패 → 기본 플레이스홀더 이미지
```

**난이도 뱃지 (F008)**

```typescript
interface DifficultyBadgeProps {
  level: Difficulty
}

const DIFFICULTY_CONFIG = {
  beginner:     { label: "초급", emoji: "🟢", color: "bg-green-100 text-green-800" },
  intermediate: { label: "중급", emoji: "🟡", color: "bg-yellow-100 text-yellow-800" },
  advanced:     { label: "고급", emoji: "🔴", color: "bg-red-100 text-red-800" },
}

// 렌더링: 이모지 + 텍스트 (예: "🟢 초급")
// 크기: 작은 뱃지 (text-xs, px-2 py-0.5, rounded-full)
```

**관련 룰 링크 (F009)**

```typescript
// 동작:
// 1. relatedRules 배열의 각 slug에 대해:
//    a. 해당 slug의 MDX 파일을 찾음
//    b. 파일이 존재하면 → 제목 + 난이도 뱃지 + 링크로 표시
//    c. 파일이 없으면 → 해당 항목 숨김 (콘솔 경고)
// 2. 표시 위치: 페이지 하단 "관련 룰" 섹션
// 3. 카드 형태: 작은 카드 리스트 (RuleCard의 축소 버전)
// 4. 최대 표시 개수: 6개 (초과 시 "더 보기" 링크 → 해당 카테고리 목록)
```

### 4.6 통합 검색 (F025)

**구현 방식:** Fuse.js 클라이언트 사이드 검색

```typescript
interface SearchDocument {
  type: "rule" | "drill" | "routine" | "glossary"
  slug: string
  category?: string
  title: string
  summary: string
  tags: string[]
  difficulty?: Difficulty
}

// 검색 인덱스 빌드:
// - 빌드 타임에 모든 MDX 프론트매터를 JSON 파일로 추출
// - 파일: public/search-index.json
// - 홈 페이지 또는 검색 모달에서 이 JSON을 fetch하여 Fuse 인스턴스 생성

// Fuse.js 설정:
const fuseOptions = {
  keys: [
    { name: "title", weight: 0.4 },
    { name: "summary", weight: 0.3 },
    { name: "tags", weight: 0.2 },
    { name: "category", weight: 0.1 },
  ],
  threshold: 0.3,        // 0 = 완전 일치, 1 = 모두 매치
  minMatchCharLength: 2, // 최소 2자 이상 입력 시 검색
  includeScore: true,
}
```

**검색 UI**

```typescript
// 트리거:
// - 헤더 검색 아이콘 클릭 → 검색 모달 오픈
// - 키보드 단축키: Ctrl+K 또는 Cmd+K
// - 모바일: 헤더 검색 아이콘 탭

// 모달 구성:
// 1. 입력 필드 (자동 포커스)
// 2. 카테고리 필터 탭: 전체 | 룰북 | 연습법 | 루틴 | 용어
// 3. 결과 리스트 (최대 10개)
//    - 각 결과: 타입 뱃지 + 제목 + 요약 일부 + 난이도
//    - 키보드 화살표 위/아래로 탐색 가능
//    - Enter 또는 클릭으로 해당 페이지 이동 + 모달 닫힘
// 4. 결과 없을 때: "검색 결과가 없습니다. 다른 키워드로 검색해보세요."

// 성능:
// - 입력 디바운스: 200ms
// - search-index.json은 첫 검색 시 1회만 fetch (메모리 캐시)
// - SSG 빌드 시 JSON 생성 → 추가 API 호출 없음
```

### 4.7 난이도 필터 (F026)

```typescript
interface FilterBarProps {
  selectedDifficulty: Difficulty | "all"
  onFilterChange: (difficulty: Difficulty | "all") => void
}

// 동작:
// 1. 목록 페이지 (룰/연습법)의 상단에 필터 바 표시
// 2. 옵션: 전체 | 🟢 초급 | 🟡 중급 | 🔴 고급
// 3. 기본값: "전체"
// 4. URL 쿼리 파라미터로 상태 유지: ?difficulty=beginner
//    - 직접 URL 입력 시 해당 필터 적용
//    - 필터 변경 시 URL 업데이트 (router.push → shallow)
// 5. 필터 결과 0건: "해당 난이도의 콘텐츠가 없습니다." 메시지
// 6. 필터는 AND 조건이 아닌 단일 선택 (한 번에 하나의 난이도만)
```

### 4.8 용어사전 (F023, F024)

**페이지:** `/glossary`

```typescript
// 구성:
// 1. 검색 입력 필드 (상단 고정)
// 2. 색인 바: ㄱ ㄴ ㄷ ... ㅎ | A B C ... Z
//    - 클릭 시 해당 초성/알파벳 섹션으로 스크롤
//    - 해당 초성에 용어가 없으면 비활성 (회색)
// 3. 용어 리스트: 초성/알파벳 그룹별 표시

// 검색 (F024):
// - 클라이언트 사이드 필터링 (Fuse.js 아닌 단순 includes)
// - 검색 대상: term, termEn, definition
// - 입력 즉시 필터링 (디바운스 100ms)
// - 검색 중에는 색인 바 숨김, 결과만 플랫 리스트로 표시

// 개별 용어 표시:
// - 한국어 (영어) — 정의
// - 관련 링크: 관련 룰, 관련 드릴 (있을 경우 칩 형태로 표시)
// - 각 용어에 앵커 ID 부여: #term-slug
//   - 룰/드릴 페이지에서 용어사전 링크 클릭 시 해당 앵커로 이동

// 초성 추출 로직:
// - 한글: 유니코드 → 초성 추출 (ㄱ~ㅎ)
// - 영문: 첫 글자 대문자
// - 숫자/특수문자: "#" 그룹
```

### 4.9 북마크 (F033)

```typescript
// 로컬스토리지 구조:
// key: "ballin-bookmarks"
// value: BookmarkStore (JSON)

interface BookmarkStore {
  version: 1
  items: BookmarkItem[]
}

interface BookmarkItem {
  type: "rule" | "drill" | "routine" | "glossary"
  slug: string          // 전체 경로 slug (e.g., "violations/traveling")
  title: string         // 표시용 제목 (오프라인 시에도 보이도록)
  addedAt: string       // ISO 8601
}

// 동작:
// 1. 북마크 버튼: 각 콘텐츠 페이지 상단 (제목 옆)
//    - 미저장: 빈 별 아이콘
//    - 저장됨: 채워진 별 아이콘
//    - 클릭 토글 (추가/제거)
// 2. 북마크 목록 보기:
//    - 헤더의 북마크 아이콘 클릭 → 사이드 패널 또는 전용 페이지
//    - 타입별 필터 가능
//    - 각 항목: 제목 + 타입 뱃지 + 추가 날짜
//    - 삭제: 스와이프 또는 X 버튼
// 3. 최대 개수: 100개 (초과 시 가장 오래된 항목 자동 삭제 + 알림)
// 4. 로컬스토리지 없는 환경 (시크릿 모드): 기능 숨김

// lib/bookmarks.ts:
function getBookmarks(): BookmarkItem[]
function addBookmark(item: Omit<BookmarkItem, "addedAt">): void
function removeBookmark(type: string, slug: string): void
function isBookmarked(type: string, slug: string): boolean
```

### 4.10 다크모드 (F034)

```typescript
// 구현:
// - Tailwind CSS dark: 클래스 기반
// - 테마 저장: 로컬스토리지 key "ballin-theme"
// - 값: "light" | "dark" | "system"
// - 기본값: "system" (OS 설정 따름)

// 토글 동작:
// 1. 클릭 순환: system → light → dark → system
// 2. <html> 태그에 class="dark" 추가/제거
// 3. 깜빡임 방지: <head>에 인라인 스크립트로 초기 테마 적용
//    (Next.js layout.tsx에서 Script 컴포넌트 사용)

// 컬러 팔레트 (CSS 변수):
// --bg-primary: light=#FFFFFF, dark=#1A1A2E
// --bg-secondary: light=#F5F5F5, dark=#16213E
// --text-primary: light=#1A1A2E, dark=#EAEAEA
// --text-secondary: light=#6B7280, dark=#9CA3AF
// --accent: light=#F97316 (주황), dark=#FB923C (밝은 주황)
// --card-bg: light=#FFFFFF, dark=#1E293B
// --border: light=#E5E7EB, dark=#334155
```

### 4.11 추천 루틴 (F031, F032)

**루틴 목록 (`/routines`)**

```typescript
// 표시 정보:
// - 제목 + 목적 (한 줄)
// - 총 소요 시간 + 난이도 뱃지
// - 포함된 드릴 개수
// - 정렬: 소요 시간 순 (짧은 것 먼저)
```

**개별 루틴 (`/routines/[slug]`)**

```typescript
// 구성:
// 1. 제목 + 난이도 뱃지 + 총 시간
// 2. 목적 설명
// 3. 드릴 순서 리스트:
//    - 각 드릴: 번호 + 제목(링크) + 세트 x 반복 + 쉬는 시간
//    - 드릴 제목 클릭 → 해당 연습법 페이지로 이동
//    - notes가 있으면 드릴 아래에 작은 텍스트로 표시
// 4. 전체 타임라인 시각화 (선택):
//    - 드릴 시간 바 + 쉬는 시간 바를 가로로 이어 표시
//    - "현재 총 ~30분" 요약

// 루틴 내 드릴 유효성:
// - drills[].slug가 실제 MDX와 매칭되지 않으면:
//   - 해당 드릴 제목은 링크 없이 텍스트만 표시
//   - 콘솔 경고
//   - title 필드를 폴백으로 사용
```

### 4.12 SEO (F012)

```typescript
// 각 페이지별 메타데이터:

// 홈
// title: "Ballin — 농구 룰 & 연습법 가이드"
// description: "FIBA/NBA 룰 비교, 연습법, 추천 루틴을 한 곳에서. 코트 위의 백과사전."

// 룰 카테고리 목록
// title: "농구 룰북 — Ballin"
// description: "기본 규칙부터 특수 상황까지, 9개 카테고리로 정리된 농구 룰"

// 개별 룰
// title: "{rule.title} — Ballin 룰북"
// description: "{rule.summary}"

// 연습법
// title: "{drill.title} — Ballin 연습법"
// description: "{drill.summary}"

// 용어사전
// title: "농구 용어사전 — Ballin"
// description: "농구 용어를 한국어/영어로 검색하고 관련 룰과 연습법을 찾아보세요."

// sitemap.xml:
// - 빌드 타임에 모든 MDX 경로를 sitemap으로 생성
// - next-sitemap 패키지 사용
// - changefreq: monthly (콘텐츠 중심이므로 자주 변경되지 않음)
// - priority: 홈 1.0, 카테고리 0.8, 개별 콘텐츠 0.6

// robots.txt:
// - 전체 허용
// - sitemap 경로 명시

// OG 이미지:
// - 기본 OG 이미지 1장 (public/og-default.png)
```

---

## 5. 라우팅

| 경로 | 페이지 | Phase | 설명 |
|------|--------|-------|------|
| `/` | 홈 | P0 | 히어로 + 바로가기 + 추천 콘텐츠 |
| `/rules` | 룰 카테고리 목록 | P0 | 9개 카테고리 카드 그리드 |
| `/rules/[category]` | 카테고리별 룰 목록 | P0 | 해당 카테고리 룰 리스트 |
| `/rules/[category]/[slug]` | 개별 룰 | P0 | MDX 본문 + FIBA/NBA 탭 |
| `/training` | 연습법 카테고리 목록 | P1 | 3개 카테고리 카드 그리드 |
| `/training/[category]` | 카테고리별 연습법 목록 | P1 | 해당 카테고리 드릴 리스트 |
| `/training/[category]/[slug]` | 개별 연습법 | P1 | MDX 본문 + 영상 + 흔한 실수 |
| `/routines` | 루틴 목록 | P2 | 추천 루틴 리스트 |
| `/routines/[slug]` | 개별 루틴 | P2 | 드릴 조합 상세 |
| `/glossary` | 용어사전 | P1 | 검색 + 초성 색인 |

**유효하지 않은 경로 처리:**

- 존재하지 않는 경로 → Next.js 기본 404 (커스텀 404 페이지 제공)
- `/rules/invalid-category` → 404
- `/rules/violations/invalid-slug` → 404
- `/rules/violations` (룰이 0개인 카테고리) → 빈 상태 메시지 표시 (404 아님)

---

## 6. 컴포넌트 맵

### 6.1 레이아웃

| 컴포넌트 | 파일 | Phase |
|----------|------|-------|
| `Header` | `components/layout/Header.tsx` | P0 |
| `Footer` | `components/layout/Footer.tsx` | P0 |
| `Breadcrumb` | `components/layout/Breadcrumb.tsx` | P0 |
| `MobileMenu` | `components/layout/MobileMenu.tsx` | P0 |

### 6.2 룰북

| 컴포넌트 | 파일 | Phase |
|----------|------|-------|
| `RuleCard` | `components/rules/RuleCard.tsx` | P0 |
| `RuleCompare` | `components/rules/RuleCompare.tsx` | P0 (탭), P2 (비교모드) |
| `CategoryCard` | `components/rules/CategoryCard.tsx` | P0 |

### 6.3 연습법

| 컴포넌트 | 파일 | Phase |
|----------|------|-------|
| `DrillCard` | `components/training/DrillCard.tsx` | P1 |
| `RoutineView` | `components/training/RoutineView.tsx` | P2 |
| `RoutineDrillItem` | `components/training/RoutineDrillItem.tsx` | P2 |

### 6.4 공통

| 컴포넌트 | 파일 | Phase |
|----------|------|-------|
| `VideoEmbed` | `components/common/VideoEmbed.tsx` | P0 |
| `DifficultyBadge` | `components/common/DifficultyBadge.tsx` | P0 |
| `InfoBox` | `components/common/InfoBox.tsx` | P1 |
| `SearchModal` | `components/common/SearchModal.tsx` | P1 |
| `FilterBar` | `components/common/FilterBar.tsx` | P1 |
| `BookmarkButton` | `components/common/BookmarkButton.tsx` | P2 |
| `ThemeToggle` | `components/common/ThemeToggle.tsx` | P2 |

### 6.5 용어사전

| 컴포넌트 | 파일 | Phase |
|----------|------|-------|
| `GlossarySearch` | `components/glossary/GlossarySearch.tsx` | P1 |
| `GlossaryList` | `components/glossary/GlossaryList.tsx` | P1 |
| `IndexBar` | `components/glossary/IndexBar.tsx` | P1 |

---

## 7. 성능 & 기술 명세

### 7.1 빌드 전략

- **SSG (Static Site Generation)** 전면 사용
- 모든 MDX 콘텐츠를 빌드 타임에 HTML로 변환
- `generateStaticParams`로 모든 경로 사전 생성
- ISR 불필요 (콘텐츠 변경 시 재빌드로 충분)

### 7.2 성능 목표

| 지표 | 목표 |
|------|------|
| Lighthouse Performance | 90+ |
| LCP (Largest Contentful Paint) | < 2.5초 |
| FID (First Input Delay) | < 100ms |
| CLS (Cumulative Layout Shift) | < 0.1 |
| 첫 로드 JS 번들 | < 100KB (gzipped) |

### 7.3 이미지 처리

- Next.js `<Image>` 컴포넌트 사용 (자동 최적화)
- YouTube 썸네일: 외부 이미지 → `next.config.ts`에 `images.remotePatterns` 설정
- 코트 다이어그램 등 정적 이미지: `public/images/`에 배치, WebP 포맷 우선
- 이미지가 없는 카드: CSS gradient 또는 아이콘 기반 플레이스홀더

### 7.4 접근성

- 시맨틱 HTML (`<main>`, `<nav>`, `<article>`, `<section>`)
- ARIA 레이블 (탭, 모달, 필터)
- 키보드 네비게이션 지원 (탭 전환, 검색 모달, 필터)
- 최소 색상 대비 비율 4.5:1 (WCAG AA)
- 포커스 링 표시 (키보드 사용자)
